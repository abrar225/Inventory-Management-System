/**
 * Application-wide client behavior.
 *
 * Kept intentionally minimal (Tech Stack: minimal JavaScript). Alpine.js
 * handles component-level interactivity and HTMX handles partial page
 * updates; this file owns only cross-cutting concerns like the theme toggle.
 */
(function () {
  "use strict";

  // App-wide scripts go here (if any)
})();
